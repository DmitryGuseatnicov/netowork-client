import React from 'react';

import { Canvas, Toolbar } from 'components';

export const App = () => {
    return (
        <div>
            <Toolbar />
            <Canvas />
        </div>
    );
};
